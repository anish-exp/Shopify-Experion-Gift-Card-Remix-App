export const loader = async ({ request }) => {
  return null;
}

export const action = async ({ request }) => {
  return null;
}

export default function Pricing() {
    return(
        <p>hello from pricing</p>
    )
}